import {IRotatable} from "../interfaces/IRotatable";
import {Vector} from "./Vector";

class Rotate {
    r: IRotatable;
    constructor(r: IRotatable) {
        this.r = r;
    }

    public Execute():void{

    }
}